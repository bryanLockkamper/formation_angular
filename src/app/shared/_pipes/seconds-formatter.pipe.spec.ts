import { SecondsFormatterPipe } from './seconds-formatter.pipe';

describe('SecondsFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
